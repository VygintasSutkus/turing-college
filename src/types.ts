export enum MeetingCategory {
    Meeting= "meeting",
    OpenSession= "open_session",
}

export enum ReviewState {
    Pending= "pending",
    InProgress= "in_progress",
    Completed= "completed",
}